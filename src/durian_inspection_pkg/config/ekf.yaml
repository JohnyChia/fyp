ekf_filter_node:
  ros__parameters:
    # ... 其他参数 ...
    odom0: /odom
    odom0_config: [true, true, false,
                    false, false, true,
                    false, false, false,
                    false, false, false,
                    false, false, false] # 简化配置，只保留必要项
    
    # 确保没有其他输入占用此接口，或者检查是否有 odom1 却未定义参数
    # 如果没有 odom1，确保不要在 launch 文件中引用它